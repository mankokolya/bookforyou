package com.library.bookforyou.util;

import com.library.bookforyou.controllers.OrderController;
import org.springframework.data.domain.Page;
import org.springframework.ui.Model;

public class PagingModel {

//    public void createPagingModel(Model model, int currentPage, String sortField, String sortDir, Page<?> page) {
//
//        OrderController.createPageabl(currentPage, sortField, sortDir, model, page.getTotalElements(), page.getTotalPages(), page);
//    }
}
